# Todo App

## Run the application

1. Install dependencies:

   Navigate inside todo-app folder
   
   ```bash
   npm install
   ```

2. Start the development server:

   ```bash
   npm run dev
   ```

3. Open the browser at the address shown in the terminal, usually:

   ```text
   http://localhost:5173/
   ```
